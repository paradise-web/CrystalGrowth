name: crystal_app
description: 晶体生长实验记录助手
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  image_picker: ^1.0.4
  flutter_markdown: ^0.6.18
  intl: ^0.19.0
  cached_network_image: ^3.3.1
  flutter_slidable: ^3.0.0
  provider: ^6.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/